function sumFirstAndLastArrayElements(arr) {
    let firttElement=arr[0];
    let lastElement=arr[arr.length -1];
    console.log(firttElement+lastElement);
}
sumFirstAndLastArrayElements([10, 17, 22, 33])